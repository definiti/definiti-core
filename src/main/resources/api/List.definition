type List[A] {
  nonEmpty(): Boolean

  isEmpty(): Boolean

  head: A

  randomElement(): A

  forall(f: A => Boolean): Boolean

  exists(f: A => Boolean): Boolean

  foldLeft[B](startValue: B, f: (B, A) => B): B
}
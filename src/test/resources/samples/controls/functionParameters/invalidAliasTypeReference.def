type Title = String

def isOtherTitle(title: Title, otherTitle: Title): Boolean {
  title == otherTitle
}

def isOther(string: String, otherTitle: Title): Boolean {
  isOtherTitle(string, otherTitle)
}